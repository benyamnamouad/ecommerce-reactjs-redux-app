import React from 'react';
import {Route, IndexRoute} from 'react-router';

import App from '../components/app';
import PostsIndex from '../components/products_index';

export default (
    <Route path="/" component={App}>
        {}
    </Route>
);